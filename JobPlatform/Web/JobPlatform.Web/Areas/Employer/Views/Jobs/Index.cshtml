@model IEnumerable<JobPlatform.Web.ViewModels.Jobs.JobSimpleViewModel>

@if (!Model.Any())
{
    <a class="btn btn-info" asp-action="AddJob">Create New</a>
}
else
{
    <div class="card shadow col-12">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered" id="userTable" width="98%" cellspacing="0">
                    <thead>
                        <tr>
                            <th>JobTitle</th>
                            <th>JobCategory</th>
                            <th>LocationCity</th>
                            <th>JobType</th>
                            <th>CompanyEmail</th>
                            <th>Description</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var job in this.Model)
                        {
                            <tr id="@job.Id">

                                <td>
                                    <p>@job.JobTitle</p>
                                </td>
                                <td>
                                    <p>@job.JobCategory</p>
                                </td>
                                <td>
                                    <p>@job.LocationCity</p>
                                </td>
                                <td>
                                    <p>@job.JobType</p>
                                </td>
                                <td>
                                    <p>@job.CompanyEmail</p>
                                </td>
                                <td>
                                    <p>@job.SanitizedContent</p>
                                </td>
                                <td>
                                    <a asp-action="EditJob" asp-route-id="@job.Id">Edit</a> |
                                    <a asp-action="Details" asp-route-id="@job.Id">Details</a> |
                                    <a asp-action="DeleteJob" asp-route-id="@job.Id">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}

@section Scripts {
    <script type="text/javascript" charset="utf8" src="~/js/jquery.dataTables.js"></script>
    <script>
        $(document).ready(function () {
            $('#userTable').DataTable();
        });
    </script>
}
